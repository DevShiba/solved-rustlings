fn main() {
    let x: i32 = -20; // This line declares a variable `x` with an explicit type annotation of `i32` and assigns it the value -20. That solves the exercise.

    println!("Number {x}");
}
