fn main() {
    let x : i32 = 10; // This line declares a variable `x` with an explicit type annotation of `i32` and assigns it the value 10. That solves the exercise.

    if x == 10 {
        println!("x is ten!");
    } else {
        println!("x is not ten!");
    }
}
